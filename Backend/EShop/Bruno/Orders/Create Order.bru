meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{buyerToken}}
}

body:json {
  {
    "productId": 1,
    "quantity": 1
  }
}
