meta {
  name: Login Buyer
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "buyer1",
    "password": "Buyer123!"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("buyerToken", res.body.token);
  }
}
